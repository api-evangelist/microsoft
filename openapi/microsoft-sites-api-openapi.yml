openapi: 3.2.0
info:
  title: Microsoft SharePoint REST Sites API
  description: REST service for remotely interacting with SharePoint data including sites, lists, list items, and document libraries using standard REST and OData protocols.
  version: '1.0'
  contact:
    name: SharePoint Developer Support
    url: https://learn.microsoft.com/en-us/sharepoint/dev/
  termsOfService: https://www.microsoft.com/en-us/legal/terms-of-use
servers:
- url: https://{tenant}.sharepoint.com/_api
  description: SharePoint REST API
  variables:
    tenant:
      default: your-tenant
      description: Your SharePoint Online tenant name
security:
- oauth2: []
tags:
- name: Sites
  description: Access SharePoint sites via Microsoft Graph
paths:
  /web:
    get:
      operationId: getWeb
      summary: Microsoft Get current web
      description: Get the properties of the current SharePoint site.
      tags:
      - Sites
      responses:
        '200':
          description: Web properties
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Web'
        '401':
          description: Unauthorized
components:
  schemas:
    Web:
      type: object
      properties:
        Id:
          type: string
        Title:
          type: string
          description: Title of the site
        Description:
          type: string
        Url:
          type: string
        Created:
          type: string
          format: date-time
        Language:
          type: integer
        WebTemplate:
          type: string
  securitySchemes:
    oauth2:
      type: oauth2
      description: Microsoft Entra ID OAuth 2.0
      flows:
        authorizationCode:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/v2.0/authorize
          tokenUrl: https://login.microsoftonline.com/common/oauth2/v2.0/token
          scopes:
            Sites.Read.All: Read items in all site collections
            Sites.ReadWrite.All: Read and write items in all site collections
externalDocs:
  description: SharePoint REST API Documentation
  url: https://learn.microsoft.com/en-us/sharepoint/dev/sp-add-ins/get-to-know-the-sharepoint-rest-service
